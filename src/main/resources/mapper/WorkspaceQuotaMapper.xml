<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmp.compute.mapper.WorkspaceQuotaMapper">

    <resultMap id="BaseResultMap" type="com.acmp.compute.entity.WorkspaceQuota">
        <id column="id" property="id"/>
        <result column="workspace_id" property="workspaceId"/>
        <result column="max_gpu_slots" property="maxGpuSlots"/>
        <result column="max_cpu_cores" property="maxCpuCores"/>
        <result column="max_memory_gib" property="maxMemoryGib"/>
        <result column="max_pods" property="maxPods"/>
        <result column="max_hours" property="maxHours"/>
        <result column="used_gpu_slots" property="usedGpuSlots"/>
        <result column="used_cpu_cores" property="usedCpuCores"/>
        <result column="used_memory_gib" property="usedMemoryGib"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO workspace_quota (id, workspace_id, max_gpu_slots, max_cpu_cores, max_memory_gib, max_pods, max_hours,
            used_gpu_slots, used_cpu_cores, used_memory_gib, created_at, updated_at)
        VALUES (#{id}, #{workspaceId}, #{maxGpuSlots}, #{maxCpuCores}, #{maxMemoryGib}, #{maxPods}, #{maxHours},
            #{usedGpuSlots}, #{usedCpuCores}, #{usedMemoryGib}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update">
        UPDATE workspace_quota
        SET max_gpu_slots = #{maxGpuSlots}, max_cpu_cores = #{maxCpuCores}, max_memory_gib = #{maxMemoryGib},
            max_pods = #{maxPods}, max_hours = #{maxHours},
            used_gpu_slots = #{usedGpuSlots}, used_cpu_cores = #{usedCpuCores}, used_memory_gib = #{usedMemoryGib},
            updated_at = CURRENT_TIMESTAMP
        WHERE workspace_id = #{workspaceId}
    </update>

    <select id="findByWorkspaceId" resultMap="BaseResultMap">
        SELECT * FROM workspace_quota WHERE workspace_id = #{workspaceId}
    </select>

    <delete id="deleteByWorkspaceId">
        DELETE FROM workspace_quota WHERE workspace_id = #{workspaceId}
    </delete>
</mapper>
