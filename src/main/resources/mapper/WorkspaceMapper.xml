<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmp.compute.mapper.WorkspaceMapper">

    <resultMap id="BaseResultMap" type="com.acmp.compute.entity.Workspace">
        <id column="id" property="id"/>
        <result column="resource_pool_id" property="resourcePoolId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="namespace" property="namespace"/>
        <result column="service_account_name" property="serviceAccountName"/>
        <result column="volcano_queue_name" property="volcanoQueueName"/>
        <result column="primary_cluster_id" property="primaryClusterId"/>
        <result column="gpu_slots" property="gpuSlots"/>
        <result column="cpu_cores" property="cpuCores"/>
        <result column="memory_gib" property="memoryGib"/>
        <result column="max_pods" property="maxPods"/>
        <result column="node_count" property="nodeCount"/>
        <result column="hardware_type" property="hardwareType"/>
        <result column="security_level" property="securityLevel"/>
        <result column="gpu_type" property="gpuType"/>
        <result column="job_types" property="jobTypes"/>
        <result column="created_by" property="createdBy"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO workspace (id, resource_pool_id, name, description,
            namespace, service_account_name, volcano_queue_name, primary_cluster_id,
            gpu_slots, cpu_cores, memory_gib, max_pods, node_count,
            hardware_type, security_level, gpu_type, job_types,
            created_by, status, created_at, updated_at)
        VALUES (#{id}, #{resourcePoolId}, #{name}, #{description},
            #{namespace}, #{serviceAccountName}, #{volcanoQueueName}, #{primaryClusterId},
            #{gpuSlots}, #{cpuCores}, #{memoryGib}, #{maxPods}, #{nodeCount},
            #{hardwareType}, #{securityLevel}, #{gpuType}, #{jobTypes},
            #{createdBy}, #{status}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update">
        UPDATE workspace
        SET name = #{name}, description = #{description}, status = #{status}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM workspace WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM workspace ORDER BY created_at DESC
    </select>

    <select id="findByResourcePoolId" resultMap="BaseResultMap">
        SELECT * FROM workspace WHERE resource_pool_id = #{resourcePoolId} ORDER BY created_at DESC
    </select>

    <delete id="deleteById">
        DELETE FROM workspace WHERE id = #{id}
    </delete>
</mapper>
