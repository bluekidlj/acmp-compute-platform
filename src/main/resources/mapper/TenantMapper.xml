<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmp.compute.mapper.TenantMapper">

    <resultMap id="BaseResultMap" type="com.acmp.compute.entity.Tenant">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="created_by" property="createdBy"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO tenant
            (id, name, description, created_by, status)
        VALUES
            (#{id}, #{name}, #{description}, #{createdBy}, #{status})
    </insert>

    <update id="update">
        UPDATE tenant
        SET name = #{name},
            description = #{description},
            status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM tenant WHERE id = #{id}
    </select>

    <select id="findByName" resultMap="BaseResultMap">
        SELECT * FROM tenant WHERE name = #{name}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM tenant ORDER BY created_at DESC
    </select>

    <delete id="deleteById">
        DELETE FROM tenant WHERE id = #{id}
    </delete>
</mapper>
