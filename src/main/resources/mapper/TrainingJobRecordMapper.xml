<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmp.compute.mapper.TrainingJobRecordMapper">

    <resultMap id="BaseResultMap" type="com.acmp.compute.entity.TrainingJobRecord">
        <id column="id" property="id"/>
        <result column="workspace_id" property="workspaceId"/>
        <result column="resource_pool_id" property="resourcePoolId"/>
        <result column="spec_id" property="specId"/>
        <result column="replicas" property="replicas"/>
        <result column="k8s_job_name" property="k8sJobName"/>
        <result column="job_name" property="jobName"/>
        <result column="status" property="status"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO training_job_record
            (id, workspace_id, resource_pool_id, spec_id, replicas,
             k8s_job_name, job_name, status, created_by, created_at, updated_at)
        VALUES
            (#{id}, #{workspaceId}, #{resourcePoolId}, #{specId}, #{replicas},
             #{k8sJobName}, #{jobName}, #{status}, #{createdBy}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update">
        UPDATE training_job_record SET status = #{status}, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM training_job_record WHERE id = #{id}
    </select>

    <select id="findByWorkspaceId" resultMap="BaseResultMap">
        SELECT * FROM training_job_record WHERE workspace_id = #{workspaceId} ORDER BY created_at DESC
    </select>
</mapper>
