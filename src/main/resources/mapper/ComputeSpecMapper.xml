<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmp.compute.mapper.ComputeSpecMapper">

    <resultMap id="BaseResultMap" type="com.acmp.compute.entity.ComputeSpec">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="display_name" property="displayName"/>
        <result column="gpu_brand" property="gpuBrand"/>
        <result column="spec_type" property="specType"/>
        <result column="resource_pool_id" property="resourcePoolId"/>
        <result column="gpu_model" property="gpuModel"/>
        <result column="gpu_memory_mb" property="gpuMemoryMb"/>
        <result column="gpu_count" property="gpuCount"/>
        <result column="cpu_cores" property="cpuCores"/>
        <result column="memory_gib" property="memoryGib"/>
        <result column="gpu_share" property="gpuShare"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO compute_spec
             (id, name, display_name, gpu_brand, spec_type, resource_pool_id,
             gpu_model, gpu_memory_mb, gpu_count, cpu_cores, memory_gib, gpu_share, description, status,
             created_at, updated_at)
        VALUES
             (#{id}, #{name}, #{displayName}, #{gpuBrand}, #{specType}, #{resourcePoolId},
             #{gpuModel}, #{gpuMemoryMb}, #{gpuCount}, #{cpuCores}, #{memoryGib}, #{gpuShare},
             #{description}, #{status},
             CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update">
        UPDATE compute_spec
        SET display_name = #{displayName},
            gpu_brand = #{gpuBrand},
            spec_type = #{specType},
            resource_pool_id = #{resourcePoolId},
            gpu_model = #{gpuModel},
            gpu_memory_mb = #{gpuMemoryMb},
            gpu_count = #{gpuCount},
            cpu_cores = #{cpuCores},
            memory_gib = #{memoryGib},
            gpu_share = #{gpuShare},
            description = #{description},
            status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM compute_spec WHERE id = #{id}
    </select>

    <select id="findByName" resultMap="BaseResultMap">
        SELECT * FROM compute_spec WHERE name = #{name}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM compute_spec ORDER BY spec_type, memory_gib DESC
    </select>

    <select id="findBySpecType" resultMap="BaseResultMap">
        SELECT * FROM compute_spec
        WHERE spec_type = #{specType}
        ORDER BY memory_gib DESC
    </select>

    <select id="findByResourcePoolId" resultMap="BaseResultMap">
        SELECT * FROM compute_spec
        WHERE resource_pool_id = #{resourcePoolId}
        ORDER BY spec_type, memory_gib DESC
    </select>

    <delete id="deleteById">
        DELETE FROM compute_spec WHERE id = #{id}
    </delete>

    <delete id="deleteAll">
        DELETE FROM compute_spec
    </delete>
</mapper>
