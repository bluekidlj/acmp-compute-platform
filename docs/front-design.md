你现在已经对于后端的平台Api 实现了核心功能的修改，现在我需要你针对这一套后端api,重新设计前端功能界面，侧边栏分为三个模块：智算运营、监控预警、集群运维、智算运营
智算运营包含 模型广场（展示常见模型） 推理服务 （展示 ，提供入口进去对话框+ 新建推理服务，基于逻辑资源池）训练管理（创建训练任务，基于volcano）
智算运营，需要包括：资源管理（用来管理三个池子和配额），逻辑池管理（管理逻辑资源池的配额操作和信息展示，展示的内容包括状态、作业这些信息，监控看板）
监控预警，包括运维监控看板（全局概览维度+节点资源维度，能够看到告警列表，报错信息，并且设置告警规则这些）
集群运维包括：负载管理（容器示例、工作部署）、存储资源管理

首先你根据我的需求，判断下是否覆盖了全部的api功能，然后你结合这个常见的算力管理治理平台的功能，判断下我的这个方案是否多余或者完备？可以进行修改设计

然后我需要演示，所以你需要提供mock 数据，让我的前端可以正常的演示操作，你注意这些数据来源和演示逻辑要和我的这个后端对的上

页面风格设计为 邮储绿色，清爽，耐看好看，b


0.1 fix

--- start 
这三类池子，共享、独享、超分，是全局唯一的，不存在异构卡管理
算力规格按照类型区分，独享规格，虚拟规格；
没有异构卡管理这个东西

你缺少一个部署推理服务这个功能？ 
也没有推理服务列表的功能,你需要补充
点进去这个推理服务，要求能够看到调用的一些指标，常见的调用监控，比如首token时延这种数据，你自己梳理下。然后提供1个对话窗口，类似于openAI那种标准对话框

工作空间和逻辑资源池合而为一，统一叫做项目，对应后端

---end 


0.2 fix 
--- start
右上角增加一个全局的集群选择框，我们始终是基于一个k8s集群来操作的
物理资源池中的三类池，是全局唯一的，用来扫描这个集群中存在的显卡的！和工作空间没有关系，后续是用来分配给逻辑资源池的
规格管理重构，列表各列，只保留规格名称，规格类型，规格详情（你现在的这个算力资源信息），操作，然后加一个新增规格的按钮，点击按钮出现弹框，填写这些信息
你的筛选框逻辑要是 正确的！

推理服务的调用监控，需要修改成曲线图，可以选择时间
我建议你根据我们后端的核心流程，修改调整优化下前端的操作逻辑
--- end 