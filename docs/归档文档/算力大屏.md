下面这段你可以**直接丢给 Agent 作为实现指令（可执行级别描述）**，已经帮你压缩成“开发可理解 + 可落地 UI 规范 + 视觉增强要求”的一段完整 Prompt。

---

# 🚀 AI算力运营大屏 · 炫酷升级版实现指令（Agent可执行）

请基于“AI算力运营数据大屏”进行升级重构，实现一个**更具未来感、科技感、空间感的3D算力指挥中心大屏UI**，整体风格参考“数字战情室 + 云计算控制塔 + AI算力神经网络”。

---

## 一、整体视觉目标（必须遵守）

请将当前传统二维数据看板升级为：

> **“3D数字孪生 + 发光神经网络 + 动态算力流动”的AI算力指挥中心**

视觉要求：

* 深色宇宙背景（深蓝 / 黑蓝渐变）
* 发光粒子网络（表示算力流动）
* 中国地图升级为“3D地形发光地图”
* GPU资源以“发光节点+能量条”表示
* 数据变化必须有动态流光效果
* 所有图表必须具备“呼吸感动画 + 动态刷新效果”

---

## 二、页面整体布局（必须严格实现）

页面分为五大区域：

```
┌────────────────────────────────────────────┐
│ ① 顶部全局态势栏（Global KPI Strip）      │
├────────────────────────────────────────────┤
│ ② 中央3D算力中国地图（核心视觉区）       │
├──────────────┬─────────────────────────────┤
│ ③ 左侧指标区 │ ④ 右侧实时事件/调度区      │
├──────────────┴─────────────────────────────┤
│ ⑤ 底部算力流动趋势 + GPU资源热力矩阵     │
└────────────────────────────────────────────┘
```

---

## 三、核心模块实现细节

---

## ① 顶部全局态势栏（必须强视觉冲击）

展示：

* 当前总算力 PFLOPS
* GPU总数
* 平均利用率
* 今日任务数
* 调度成功率
* 成本下降比例

要求：

* 每个指标必须“数字滚动增长动画”
* KPI卡片必须带发光边框 + 微粒子流动效果

---

## ② 核心区域：3D算力中国地图（最重要）

必须升级当前2D地图为：

### 视觉要求：

* 中国地图具备“3D浮雕地形效果”
* 每个城市是一个“发光算力节点”
* 节点颜色代表负载：

  * 绿色：空闲
  * 蓝色：正常
  * 橙色：高负载
  * 红色：过载

---

### 动态效果：

* 城市之间有“算力流光线路”
* 任务调度时出现“粒子流动动画”
* 节点实时跳动（heartbeat effect）

---

### 信息弹窗（hover）

显示：

* GPU数量
* 当前利用率
* 任务数量
* 延迟

---

## ③ 左侧指标区（资源态势）

包含以下模块：

### GPU资源结构图（环形图）

* NVIDIA
* 华为昇腾
* 海光DCU
* 其他

---

### 资源池状态

* 总资源池
* 已用资源
* 逻辑资源池占比

---

### 负载趋势图（折线图）

必须模拟：

* 白天高推理负载
* 夜间高训练负载

---

## ④ 右侧实时调度与事件区（核心动态）

必须包含：

### 实时调度流（关键）

显示：

* 新任务进入
* GPU分配过程
* 调度成功/失败

要求：

👉 必须有“流动动画日志”

---

### AI推理服务调用监控

* 成功率
* 调用次数
* 失败次数

---

### TOP模型调用排行

例如：

* DeepSeek
* Qwen
* Llama
* 自研模型

---

## ⑤ 底部区域（趋势 + 热力）

### GPU资源热力矩阵（核心视觉）

* 横轴：时间
* 纵轴：GPU节点
* 颜色：利用率

要求：

👉 类似“战场热力图”

---

### 全局算力趋势图

展示：

* PFLOPS变化
* 利用率变化
* 成本变化

---

## 四、必须增加的“高级炫酷能力”（重点）

### 1. 算力流动动画系统

* GPU之间资源流动可视化
* 任务调度路径可视化

---

### 2. 实时模拟引擎（Mock）

必须实现：

* 每5秒数据变化一次
* 模拟负载波峰波谷
* 模拟节点故障与恢复

---

### 3. AI控制感视觉

所有操作必须表现为：

* AI调度中
* 算力优化中
* 资源重平衡中

---

## 五、UI风格关键词（必须遵守）

* Cyberpunk Enterprise
* AI Command Center
* Neural Network Glow
* Data Ocean Flow
* Futuristic Control Tower

---

## 六、最终目标效果（一句话）

> 打造一个“银行级AI算力战争指挥中心大屏”，让用户感觉自己在操作一个实时运行的国家级算力系统，而不是普通监控面板。

---

如果你下一步需要，我可以帮你再升级一版更狠的：

* 👉 “带3D建模坐标的前端实现拆解（Three.js版）”
* 👉 或“直接给React组件拆分结构”
* 👉 或“领导汇报讲解词（1分钟震撼版）”
